<!-- format with comment & int -->
<p>{{ _nx_noop('There is one cat', 'There are %d cats', 'my context', 'the number of cats')|format(cats) }}</p>
<!-- format with comment & float -->
<p>{{ _nx_noop('There is one dog', 'There are %d dogs', 'my context', 'the number of dogs')|format(dogs) }}</p>
<!-- format with comment & scientific notation -->
<p>{{ _nx_noop('There is one thing', 'There are %d things', 'my context', 'the number of things')|format(things) }}</p>

<!-- simple with comment & int -->
<p>{{ _nx_noop('There is one lizard', 'There are %d lizards', 'my context', 'the number of lizards') }}</p>
<!-- simple with comment & float -->
<p>{{ _nx_noop('There is one frog', 'There are %d frogs', 'my context', 'the number of frogs') }}</p>
<!-- simple with comment & scientific notation -->
<p>{{ _nx_noop('There is one bagel', 'There are %d bagels', 'my context', 'the number of bagels') }}</p>
